# Daily Calorie Intake Calculator 🍎

Простое и удобное приложение на Flutter для расчета суточной нормы калорий на основе ваших параметров, уровня активности и целей.

## 📱 Описание

Приложение помогает пользователям определить, сколько калорий им необходимо потреблять ежедневно. Расчет строится на основе проверенных формул (Миффлина-Сан Жеора) и учитывает индивидуальные особенности организма и образ жизни.

Приложение проводит пользователя через простой опрос:
1.  Ввод базовых параметров (Пол, Вес, Рост, Возраст).
2.  Выбор цели (Похудение, Поддержание, Набор массы).
3.  Оценка физической активности.
4.  Получение результата.

## 🛠 Технологический стек

* **Flutter & Dart**: Основной фреймворк и язык.
* **Native State Management**: Реализован через `InheritedWidget` (`UserDataProvider`) для эффективной передачи данных вниз по дереву виджетов без сторонних зависимостей.
* **Material Design**: Использование стандартных компонентов UI от Google.

## 📂 Структура проекта

```text
lib/
├── core/
│   └── app_routes.dart           # Маршруты навигации
├── models/
│   └── user_model.dart           # Модель данных пользователя
├── provider/
│   ├── user_data_provider.dart   # InheritedWidget для доступа к данным
│   └── user_data_state.dart      # Виджет-обертка для хранения состояния
├── screens/
│   ├── calorie_title_screen.dart # Стартовый экран
│   ├── calorie_basic_parameters_screen.dart # Ввод веса/роста/возраста
│   ├── calorie_target_screen.dart # Выбор цели
│   ├── calorie_activity_screen.dart # Выбор активности
│   └── calories_result_screen.dart  # Экран результатов
├── utils/
│   └── calorie_calculator.dart   # Логика расчетов (BMR, TDEE)
└── main.dart                     # Точка входа
🧮 Логика расчетов
Расчеты производятся в классе CalorieCalculator:

BMR (Базовый обмен веществ):

Для мужчин: (10 × вес) + (6.25 × рост) - (5 × возраст) + 5

Для женщин: (10 × вес) + (6.25 × рост) - (5 × возраст) - 161

TDEE (Суточный расход энергии):

BMR умножается на коэффициент активности (от 1.2 до 1.9).

Корректировка под цель:

📉 Похудение: TDEE × 0.85 (-15%)

⚖️ Поддержание: TDEE × 1.0

📈 Набор массы: TDEE × 1.15 (+15%)
